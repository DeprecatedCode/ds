'DefaultScript Help'              @log
'Usage: node ds path/to/file.ds'  @log
'Dev: node . examples/*.ds'       @log
'All injectables:'                @log

# Object.keys(ds.global).map(function (x) {
#     return '@' + x;
#   }).join(', '));
# };
