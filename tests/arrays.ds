cart: ['apple', 'banana', 'carrot', 'apple']

'There are' cart.length ' items in the cart.' @Console.log

isFruit: {@it ['apple', 'banana', 'orange'].contains}

fruitOnly: cart.filter isFruit
nonFruit: cart.exclude isFruit

`The cart has ${fruitOnly.length} fruits and
              ${nonFruit.length} other items in it.` @Console.log

`The second item in the cart is a delicious yellow ${2 cart}.`;
